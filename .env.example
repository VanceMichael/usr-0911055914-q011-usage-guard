# 服务端口
PORT=8080
# PostgreSQL 连接串（权威存储：合同、阶梯价、事件、决定审计、周期累计）
DATABASE_URL=postgres://guard:guard@localhost:5432/guard
# Redis 连接串（短窗口限速计数 + 周期累计缓存，重启后从 PG 重建）
REDIS_URL=redis://localhost:6379/0
# occurred_at 早于接收时间超过该秒数 → 标记为补发事件
BACKFILL_SKEW_SECONDS=60
